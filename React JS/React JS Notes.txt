                             React JS Notes
                            ================
22-1-26
========

React-2601 10 AM
----------------
ES6 & ES7 (Version of Javasript) - ECMA Script Organization who introduces concepts to Javascript programming language.
Typescript
React Js
ES6 & ES7
Arrow Functions
Let and Const
Template Literals
Destructuring
Default Parameters
Spread and Rest Operators
Object Literal Enhancements
Modules
Promises and Async/Await
Array Methods (map, filter, reduce)
Promises and Fetch API
Array Methods (Function):
------------------------
forEach(): For iterating each value present in array.
map()
filter()
reduce()
find()
forEach():
    Syntax:
        arrayName.forEach(function(value, index) {
            // Statements....
        });
map(): Used to format each value present in array.
    Syntax:
        const formattedArr = arrayName.map(function(value, index) {
            // Statements....
            return formattedValue;
        });
function functionName(paramName1) {
}
filter: 
    Syntax:
        arrayName.filter(function(value, index) {
            return condition;
        });
find: Used to retrieve value from array.
    Syntax:
        arrayName.find(function(value, index) {
            return condition;
        });

23-1-26
========

Reduce:
    Syntax:
        arrayName.reduce(function(acculumator, value) {
            return acculumator + value;
        }, initialValue)
        acculumator - undefined
        acculumator - 0
Arrow function: 
    function {} return - => 

27-1-26
========

Destructuring
Default Parameters
Spread and Rest Operators
Object Literal Enhancements
Modules
Default Parameters: Assigning default values to the parameters of the function.
------------------
    Syntax:
        function functionName(parameter1 = defaultValue1, parameter2 = defaultValue2) {
            // Statements...
        }
        functionName()
        functionName(value1)
        functionName(value1, value2)
Spread and Rest Operators:
Spread Operator: Extract values from array or object as individual values.
    Syntax:
        const arr = [value1, value2];
        const arr2 = [value3, ...arr]
                    [value3, value1, value2]
        const arr2 = [...arr, value3]
            [value1, value2, value3]
Rest Operator: Store rest of the values passed to the function definition.
    Syntax:
        function functionName(param1, param2) {
        }
        functionName(value1, value2, value3);
Object Literal Enhancements:
---------------------------
If variable name and key name in object is same. Write only key name.
    Syntax:
        const variableName = value;
        const objectName = {
            keyName
        };
Modules:
fileName1.js
    export const variableName = value;
    export const functionName = () => {
    };
fileName2.js
import { variableName, functionName } from 'fileName1.js';
    console.log(variableName);
    functionName();

28-1-26
========

Destructuring of Object & array
Object
    Syntax:
        const obj = {
            key1: value1,
            key2: value2,
            key3: value3,
            keyN: valueN
        };
        const variableName1 = obj.key1;
        const variableName2 = obj.key2;
        const variableName3 = obj.key3;
        const { variableName1, variableName2, variableName3 } = obj;
Array
Javascript - Dynamic Typing .js
Typescript: - Static Typing .ts
Types
const variableName = value;
    let name: string = 'Digital Edify';
    name = 45;
    let user: object  = {
        name: 'Bhargav',
        id: 1,
        city: 'Hyderabad'
    };
    user = {
    }
    interface

29-1-26
========

Typescript: It is an Object oriented programming language.
> It is superset of Javascript.
    filename.ts
    filename.js - Node Js (It's a javascript runtime environment)
        Windows - Chrome
        Android - 
        V8 - Javascript Engine which is installed in Chrome
        npm - node package manager (It helps us in installing or uninstalling packages in the laptop/project)
            react - Meta
            bootstrap
            jquery
            angular - Google
            vue
            javascript (Node Js & Express Js), Java (Spring Boot), Python (Django), Go, C#Net, PHP, 
        To install a package:
        Local:
            npm install package-name
        Global:
            npm install -global package-name
            npm install -g typescript
                -D
            dependencies - Used in development & production phase.
 
            devDependencies - Used only in development phase.
        Typescript ?
        Javascript - 
        tsc - typescript complier - It verifies weather syntax is correct or not, if syntax is correct it converts typescript code to javascript.
            x.ts -> x.js
        node x.js

30-1-26
========
--------------------------------------------------------------------------------
Root / Config
--------------------------------------------------------------------------------
package.json
NPM project manifest: app name (ecommerce-app), scripts (npm run dev, build, lint, preview),
dependencies (React 19, react-dom), and dev tools (Vite, TypeScript, ESLint).

index.html
Single HTML entry: defines <div id="root"> and loads the app via
<script type="module" src="/src/main.tsx">. Vite serves this and injects the built JS.

vite.config.ts
Vite config: enables the React plugin so Vite can compile JSX/TSX and support Fast Refresh.

.gitignore
Tells Git to ignore node_modules, dist, logs, editor files, etc.

README.md
Project readme for the Vite + React + TypeScript template (setup, scripts, optional ESLint/React Compiler notes).

eslint.config.js
ESLint flat config: JS/TS recommended rules, React Hooks, React Refresh for Vite;
applies to **/*.{ts,tsx}.

--------------------------------------------------------------------------------
TypeScript Config
--------------------------------------------------------------------------------
tsconfig.json
Root TS config: no files compiled here; only references tsconfig.app.json and tsconfig.node.json.

tsconfig.app.json
Config for app code in src/: ES2022, DOM types, ESNext modules, "jsx": "react-jsx",
strict checks; used by the editor and build.

tsconfig.node.json
Config for Node/tooling (e.g. vite.config.ts): ES2023, Node types; separate from browser app code.

--------------------------------------------------------------------------------
Source (src/)
--------------------------------------------------------------------------------
main.tsx
App entry: imports global CSS and App, uses createRoot to mount React into #root,
and wraps the tree in <StrictMode>.

App.tsx
Root React component: default Vite+React UI (logos, counter with useState,
"Edit src/App.tsx" message). This is where you'd add e-commerce layout and pages.

App.css
Styles for the default App UI: #root layout, .logo size/hover, logo-spin animation,
.card, .read-the-docs.

index.css
Global styles: :root (font, colors, light/dark), base styles for body, a, button, h1,
and a prefers-color-scheme media query.

--------------------------------------------------------------------------------
Other
--------------------------------------------------------------------------------
public/vite.svg
Favicon/asset served at /vite.svg (referenced in index.html).

src/assets/react.svg
React logo image imported in App.tsx.